defmodule LivingWorldWeb.PageControllerTest do
  use LivingWorldWeb.ConnCase

  # TODO Once UI is decided, update these tests
  # test "GET /", %{conn: conn} do
  #   # conn = get(conn, "/")
  #   # assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  #   assert true
  # end
end
